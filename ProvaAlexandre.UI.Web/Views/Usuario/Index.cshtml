@model IEnumerable<ProvaAlexandre.Dominio.Usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Cadastrar")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UsuarioNome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Senha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UsuarioNome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Senha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Editar", new { id=item.UsuarioId }) |
            @Html.ActionLink("Details", "Detalhes", new { id=item.UsuarioId }) |
            @Html.ActionLink("Delete", "Excluir", new { id=item.UsuarioId })
        </td>
    </tr>
}

</table>
